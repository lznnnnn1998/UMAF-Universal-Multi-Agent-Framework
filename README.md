# UMAF — Universal Multi-Agent Framework

A modular, extensible multi-agent framework supporting autonomous code generation and AI-powered research with LLM-based review loops. Built on [LangChain](https://www.langchain.com/) + [LangGraph](https://langchain-ai.github.io/langgraph/).

## Overview

UMAF orchestrates multiple autonomous agents that collaborate through structured pipelines. Each agent reasons, uses tools, and produces verifiable output — mimicking the workflow of a human engineering or research team.

### Two Pipelines

| Pipeline | Command | Flow |
|----------|---------|------|
| **Coder** | `-m coder` | Coder implements → Reviewer reviews → loop until pass or max cycles |
| **Research** | `-m research` | Head decomposes topic → Workers research in parallel → Reviewer scores → Writer generates LaTeX |

### Two LLM Backends

| Backend | Flag | Mechanism |
|---------|------|-----------|
| **DeepSeek API** | `-b deepseek` | LangChain `ChatOpenAI`, JSON tool-call loop |
| **Claude CLI** | `-b claude_cli` | Subprocess `claude -p`, native tool calling, DeepSeek proxy |

## Architecture

```
main.py
├── graph.py (coder/reviewer pipeline)
│   └── agent.py (autonomous agent loop)
│       ├── llm.py (DeepSeek API / Claude CLI subprocess)
│       └── tools.py (5 tools: read, write, shell, web_search, call_claude)
│
└── research/graph.py (research pipeline)
    ├── research/head_agent.py (task decomposition)
    ├── research/worker_agent.py (parallel research)
    ├── research/reviewer_agent.py (scoring & ranking)
    └── research/writer.py (LaTeX generation)
```

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Configure DeepSeek backend (default)
echo 'DEEPSEEK_API_KEY=your-key-here' > .env

# Or use Claude CLI backend (routes through DeepSeek proxy)
# Edit claude_env_sample.json with your credentials

# Coder mode: generate code with review
python3 main.py -m coder "write a Python script to parse CSV files"

# Research mode: generate a LaTeX research proposal
python3 main.py -m research -b claude_cli "model quantization: QAT, PTQ, stochastic rounding"
```

### CLI

```
python3 main.py [--mode coder|research] [--backend deepseek|claude_cli] [--working-dir PATH] "requirement"
```

If no argument is given, reads from stdin or interactive prompt.

## Features

- **Autonomous tool use**: agents can read/write files, run shell commands, search the web, and delegate to nested Claude instances
- **Multi-agent review loops**: coder↔reviewer with up to 5 cycles; research pipeline with dedicated scoring
- **Circuit breakers**: agent-level error spiral detection, graph-level thread timeouts, deduplication of redundant worker output
- **Parallel workers**: research workers run concurrently via `ThreadPoolExecutor` (max 4)
- **Graceful fallbacks**: every pipeline stage has a Python fallback — decomposition, research, scoring, LaTeX generation
- **Working directory sandboxing**: all file operations scoped to a temp directory
- **Python 3.9+ compatible**

## Tools

| Tool | Description | Timeout |
|------|-------------|---------|
| `read_file(path)` | Read file contents | — |
| `write_file(path, content)` | Write file (creates parent dirs) | — |
| `run_command(command)` | Shell command | 30s |
| `call_claude(prompt)` | Delegate to Claude CLI subprocess | 120s |
| `web_search(query)` | DuckDuckGo Lite search | 15s |

All tools accept `working_dir` for path sandboxing.

## Research Pipeline Details

```
Head Agent (120s timeout)
  └─ Decomposes topic → 5-7 specific sub-topics (LLM or fallback)

Worker Agents (4 concurrent, 300s timeout each)
  └─ Each researches one sub-topic → writes research_NN_Title.md

Reviewer Agent
  └─ Scores all outputs on 5 dimensions (depth, accuracy, relevance,
     clarity, originality, each 1-10) → writes scoring_report.json

Writer
  └─ Synthesizes top 3 into publication-quality LaTeX → research_proposal.tex
```

### Verified Results (May 2026)

Topic: *"model quantization: QAT, PTQ, stochastic rounding"*

- 5 specific sub-tasks generated by LLM (not fallback)
- 5/5 workers completed successfully (100%)
- Top scores: **46/50** (comparative analysis), 44/50 (QAT), 44/50 (PTQ)
- Real LLM-based scoring with detailed per-dimension justifications

## Configuration

### DeepSeek Backend
Set `DEEPSEEK_API_KEY` in `.env`.

### Claude CLI Backend
Requires the `claude` CLI installed (`npm install -g @anthropic-ai/claude-code`). Copy `claude_env_sample.json` and set credentials. The subprocess needs broad tool permissions:

```json
// ~/.claude/settings.json
{
  "permissions": {
    "WebSearch": "*",
    "Bash": "*",
    "Read": "*",
    "Write": "*",
    "Edit": "*"
  }
}
```

Set `CLAUDE_ENV_PATH` env var for a custom config path.

## Limitations

- **Worker file output**: `claude -p` may not always write to the exact filename requested; files can appear under different names
- **Nested claude calls**: worker tasks use nested `claude -p` for deep research, adding latency (2-3 min per call)
- **Web search fragility**: DuckDuckGo Lite HTML scraping is fragile to layout changes
- **DeepSeek JSON parsing**: enforcing structured tool calls via JSON prompts is less reliable than native tool calling
- **No cancellation**: already-running parallel workers can't be cancelled; dedup is post-hoc only

## Possible Optimizations

### Near-term (low effort, high impact)

1. **Worker prompt simplification**: Remove nested `claude -p` delegation from worker tasks. Let the outer `claude -p` handle research directly rather than spawning nested subprocesses. Estimated speedup: **2-3x** per worker.

2. **DuckDuckGo → dedicated search API**: Replace HTML scraping with a proper search API (SerpAPI, Brave Search) for reliability and structured results.

3. **Caching**: Cache decomposition results and worker outputs keyed by topic hash. Repeated runs on the same topic skip redundant work.

4. **Timeout tuning**: Current 300s worker timeout is conservative. Test with 180s + simpler prompts to reduce pipeline wall-clock time.

### Medium-term (moderate effort)

5. **DeepSeek tool calling**: Use LangChain's native `bind_tools()` instead of JSON prompt engineering for the deepseek backend. This gives the deepseek path the same reliability as the claude_cli path.

6. **Worker result merging**: When multiple workers produce semantically identical output (detected by dedup), merge their findings rather than discarding duplicates.

7. **Incremental LaTeX compilation**: Validate generated `.tex` files with `pdflatex --draftmode` and surface errors back to the writer for correction.

8. **Streaming output**: Stream agent progress to the CLI (currently all output arrives at pipeline completion).

### Longer-term (higher effort, architectural)

9. **Dynamic worker allocation**: Vary the number of workers based on topic complexity (estimated by keyword count or head agent confidence).

10. **Reviewer feedback loop**: Feed reviewer critique back to workers for a second research pass before final scoring — analogous to the coder↔reviewer loop.

11. **Multi-model routing**: Use smaller/faster models for decomposition and larger models for deep research and scoring.

12. **Plugin system**: Let users register custom agent roles and pipeline topologies without modifying core framework code.

## Future Work

### Phase 3 — Production Hardening
- Comprehensive test suite (unit tests for each module, integration test for full pipeline)
- Structured logging with trace IDs across agent boundaries
- Graceful shutdown: signal handling to clean up subprocesses on interrupt
- Configurable timeouts and retry policies via config file

### Phase 4 — New Pipeline Topologies
- **Debate mode**: two agents argue opposing positions, judge agent synthesizes consensus
- **Fact-check mode**: writer agent drafts, fact-checker verifies each claim against search results
- **Self-improving code**: coder writes tests first, implements, reviewer checks, coder refactors based on review

### Phase 5 — Observability & UI
- Web dashboard showing pipeline progress, agent transcripts, and scoring breakdowns
- Cost tracking: token usage and API call counts per pipeline run
- Export formats: PDF (via `pdflatex`), HTML, Markdown in addition to LaTeX

## Project Structure

```
.
├── main.py                  # CLI entry point
├── agent.py                 # Autonomous agent loop (both backends)
├── llm.py                   # DeepSeek + Claude CLI LLM backends
├── tools.py                 # 5-tool toolkit with sandboxing
├── graph.py                 # Coder/reviewer LangGraph pipeline
├── claude_config.py         # Claude CLI env injection
├── claude_env_sample.json   # Template for Claude CLI credentials
├── requirements.txt         # Python dependencies
├── CLAUDE.md                # Project documentation for AI assistants
└── research/
    ├── __init__.py
    ├── graph.py             # Research pipeline LangGraph
    ├── head_agent.py        # Topic decomposition
    ├── worker_agent.py      # Parallel research execution
    ├── reviewer_agent.py    # 5-dimension scoring
    └── writer.py            # LaTeX proposal generation
```

## License

MIT
